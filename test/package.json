{
  "scripts": {
    "lint": "tslint ts_test/src/**/*.ts",
    "lint:fix": "npm run lint -- --fix",
    "generate": "(cd ts_test && rm -rf generated && ./generate.sh) && (cd js_test && rm -rf generated && ./generate.sh)",
    "mocha": "mocha --require source-map-support/register ./ts_test/build/**.js ./js_test/src/**.js",
    "build": "cd ts_test && rm -rf ./build && tsc",
    "test": "npm run lint && npm run generate && npm run build && npm run mocha"
  }
}
